function Node(val, left, right) {
  this.val = val;
  this.left = left;
  this.right = right;
}

function buildThree(node, threeLevel) {
  let levelNum = 1;
  let left = new Node(levelNum);
  let right = new Node(levelNum);

}

function walkThree(root) {

}

let node = new Node(0, null, null);
let three = buildThree(node, 4);
